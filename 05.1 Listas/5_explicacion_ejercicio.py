#Crea una lista llamada número con sus respectivos elementos.
numeros = [8, 15, 3, 22, 7]
 
#Se elimina el número más grande de la lista "numeros"
numeros.remove(max(numeros))

#Se imprime la lista resultante por pantalla.
print(numeros)