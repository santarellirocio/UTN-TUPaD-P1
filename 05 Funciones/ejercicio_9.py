import utils
# Le pedimos al usuario que ingrese la temperatura en grados Celsius
temperatura_celsius = float(input("Ingrese una temperatura en °C: "))
# Llamamos a la función para mostrar el resultado de Celcius a Fahrenheit
utils.celsius_a_fahrenheit(temperatura_celsius)